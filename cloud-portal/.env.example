# Copia questo file in .env e valorizza i campi prima di eseguire docker compose up

# Genera con: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

POSTGRES_DB=cloud_portal
POSTGRES_USER=cloud_portal
POSTGRES_PASSWORD=changeme

BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=changeme

PORTAL_PORT=8000
