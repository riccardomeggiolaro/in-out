Sistemare crush in-out quando vengono passate variabili non esistenti nel report e aggiungere
- weight1.type | default(‘1° Peso’)
- weight1.type | default(‘Pesata Entrata’)
- weight1.type | default(‘Entrata’)